# Named 00- to sort before the cloud-init drop-in (50-cloud-init.conf):
# sshd applies the first value it reads for each keyword, so this file
# must be read first to own the effective policy.
PasswordAuthentication no
PermitRootLogin no
PubkeyAuthentication yes
KbdInteractiveAuthentication no
