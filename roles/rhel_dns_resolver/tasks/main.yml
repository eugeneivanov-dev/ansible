---
- name: Assert default IPv4 interface fact is available
  ansible.builtin.assert:
    that:
      - ansible_facts['default_ipv4'] is defined
      - ansible_facts['default_ipv4'].interface is defined
      - ansible_facts['default_ipv4'].interface | length > 0
    fail_msg: >-
      default_ipv4.interface fact is not available — cannot determine
      the primary interface for resolver configuration

- name: Get connection name for default interface
  ansible.builtin.command: >
    nmcli -g GENERAL.CONNECTION device show
    {{ ansible_facts['default_ipv4'].interface }}
  register: rhel_dns_resolver_device_conn
  changed_when: false
  check_mode: false

- name: Assert connection name is not empty
  ansible.builtin.assert:
    that:
      - rhel_dns_resolver_device_conn.stdout | trim | length > 0
    fail_msg: >-
      No NetworkManager connection is associated with interface
      {{ ansible_facts['default_ipv4'].interface }} — refusing to guess

- name: Determine primary connection
  ansible.builtin.set_fact:
    rhel_dns_resolver_connection: "{{ rhel_dns_resolver_device_conn.stdout | trim }}"

- name: Read current DNS servers
  ansible.builtin.command: nmcli -g ipv4.dns connection show "{{ rhel_dns_resolver_connection }}"
  register: rhel_dns_resolver_current_dns
  changed_when: false
  check_mode: false

- name: Read current DNS search domain
  ansible.builtin.command: nmcli -g ipv4.dns-search connection show "{{ rhel_dns_resolver_connection }}"
  register: rhel_dns_resolver_current_search
  changed_when: false
  check_mode: false

- name: Align resolver settings to target
  ansible.builtin.command: >
    nmcli connection modify "{{ rhel_dns_resolver_connection }}"
    ipv4.dns "{{ rhel_dns_resolver_servers }}"
    ipv4.dns-search "{{ rhel_dns_resolver_search }}"
  when: >
    rhel_dns_resolver_current_dns.stdout | replace(',', ' ') != rhel_dns_resolver_servers
    or rhel_dns_resolver_current_search.stdout | replace(',', ' ') != rhel_dns_resolver_search
  changed_when: true
  notify: Reactivate connection
