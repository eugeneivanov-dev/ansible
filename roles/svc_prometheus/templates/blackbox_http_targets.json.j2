[
{% for entry in vault_blackbox_http_targets %}
  {
    "targets": ["{{ entry.target }}"],
    "labels": {
{% for key, value in entry.labels.items() %}
      "{{ key }}": "{{ value }}"{% if not loop.last %},{% endif %}

{% endfor %}
    }
  }{% if not loop.last %},{% endif %}

{% endfor %}
]
