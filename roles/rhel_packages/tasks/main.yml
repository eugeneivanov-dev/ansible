---
- name: Ensure baseline packages are installed
  ansible.builtin.dnf:
    name:
      - vim
      - bash-completion
      - tar
      - unzip
      - policycoreutils-python-utils
      - bind-utils
      - openssl
    state: present
